# Phase 7A Level 1：项目参数 1RC pure DNN 消融报告

## 结论

Level 1 判定：**Level 1 未通过**。是否允许进入 Level 2：**否**。

## 实验边界

- 状态仅为 `SOC` 与主导极化电压 `Vp`；使用项目 OCV、5 Ah 容量、`R0=0.02134098 Ω`、`R1=0.00911829 Ω`、`τ1=17.243602 s`。
- 仅保留 `0–10 A` 电流边界与 `Vtr≤4.20 V`；未加入斜率、温度、扰动、DFN 或压力场。
- 候选教师轨迹 240 条、每条 8 步；训练/验证/测试按轨迹冻结隔离。

## 教师数据与确定性审计

- 接受轨迹：240/240（100.00%），有效样本 1920。
- 100 个代表状态各运行 15 个 warm start；fallback 总数为 0 的检查：True。
- 近最优多值状态比例：0.00%（门槛 5%）。
- 近最优第一动作极差 P95：0.000000 A（门槛 0.05 A）。
- 教师审计总判定：通过。

## pure DNN 与同模型闭环

- 五种子冻结测试 NRMSE 范围：0.0672%–0.1255%（门槛 <1%）。
- 五种子同模型闭环平均电流 NRMSE 范围：8.8788%–9.0725%（门槛 <1%）。
- 最低在线加速：2310.9×。

## 阶段门槛

```json
{
  "teacher_determinism_passed": true,
  "offline_test_passed": true,
  "same_model_closed_loop_passed": false
}
```

## 失败定位

未检测到明确的 SOC 末端覆盖缺口。

最终判定严格要求“教师确定性、冻结离线测试、同模型闭环”三者同时通过；本报告不包含任何 Level 2 或更高层级实验。
